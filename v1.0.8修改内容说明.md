# V1.0.8 新增与修改内容说明

> 文档日期：2026-09-09  
> 适用版本：[V1.0.8.html](V1.0.8.html) 及对应 Web 版。  
> 本文整理 V1.0.8 已落地的新增功能、交互调整和修复，以本轮最终实现为准；未采用的设计方案不计入正式功能。

## 1. 版本概览

V1.0.8 主要围绕“日常任务可以独立处理，复杂问题再升级为 Case”完善任务管理体验。

| 模块 | 本版主要内容 |
| --- | --- |
| 我的任务 | 我负责 / 我分派 / 我参与视角，保留三个状态模块，支持业务筛选、分组、历史 Case 任务及速览抽屉 |
| 新建任务 | 新增独立创建页面，默认直接选负责人，可在任务分派右上角开启高级查找 |
| 任务处理 | 日常任务支持接受、拒绝、Workspace 保存、完成、中止及升级 |
| 退回重做 | 发起人或负责人主管可将已完成任务退回进行中 |
| 日志与评论 | 两类任务详情统一记录已保存操作、字段修改、评论和回复 |
| 详情布局 | 附件紧随任务信息，日志置于内容末尾，底部按钮统一 |
| Case 关联 | 修复 DIMM / CAMM 与拆分 IC Case 的双向关联及旧数据回填 |
| EA Agent | 新增可拖动的悬浮机器人、聊天窗口及整页展开交互 |
| 日常任务范例 | 本版累计 11 条初始范例，最近补充 3 条待接受、3 条进行中 |

**与已有能力的区别：**“我的任务”入口、Case 侧 Workspace、部分状态历史、评论及关联 Case 能力在早期版本中已有。本版重点是新增独立日常任务体系，并完善任务级持久保存、权限校验和上下文保留，而非重新引入所有基础概念。

## 2. 我的任务工作台

### 2.1 关系视角与状态模块

- 标题右侧提供“我负责 / 我分派 / 我参与”，默认我负责；原“我协作”统一为“我参与”。新建 Case / 任务表单按最新要求使用“协作者”，不改变“我参与”入口或内部存储字段。
- 我负责：当前任务负责人是我。
- 我分派：日常任务创建人或 Case 任务明确分派人是我，且负责人是其他人；不再以缺失分派人的 Case Owner 猜测实际分派者。
- 我参与：任务参与人包含我，且负责人不是我。旧 Case 任务缺少任务级参与人字段时沿用 Case 参与人；显式空列表不自动扩大参与范围。
- 保留“待接受 / 进行中 / 已完成”三个完整状态卡片，放在标题与关系切换下方。点击筛选，再次点击已选卡片回到全部状态；不增加另一套状态下拉或历史主导航。
- 进入“我的任务”页面时默认选中“待接受”，任务归属仍为“全部”、关系仍为“我负责”；没有待接受任务时显示空态，不自动切换状态。速览关闭保留当前选择；“清除筛选”仍回到全部状态。
- 数量按人员关系、任务归属和业务条件一致统计；点击状态卡片只筛选列表，不把另外两个卡片数量清零。
- 已完成卡片的辅助文案改为“成果与记录”。无状态筛选时可查已拒绝、已中止等任务。

### 2.2 业务筛选、历史与分组

- 状态模块下方是任务归属筛选“全部 / 日常任务 / Case 任务”，默认全部。
- “我的任务”页面内搜索入口暂时移除，不参与列表及数量筛选；顶部全局搜索保持不变。
- 可展开“业务筛选”：任务涉及范围（产品 / Project / TF / 无）、具体对象、所属 Case、创建或分派人。“无”替代原“无关联对象”，筛选含义不变。
- “全部”与“Case 任务”均显示完整业务筛选字段，支持“所属 Case”筛选，两者切换保留已选 Case；仅“日常任务”隐藏并清除该条件及标签，避免隐藏条件影响列表和数量。其余业务条件保留，筛选区相应采用三列或四列排列，不留空位。
- 日常任务使用自身涉及对象；导入的 Case 任务保留直接引用，同时可读取所属 Case 的明确产品关联。卡片及速览区分直接关联与“来自 Case”。
- Case 中只有 Project 名称的旧数据标记为“名称记录”，不冒充 Project ID，不通过任务标题推断产品或项目。
- 支持不分组、按截止日期、按负责人、按 Case、按涉及对象分组。分组后全局分页，每页最多8条，不因多个业务关系重复列出同一任务；选择具体对象时优先按该对象分组，其他情况优先直接关联。
- 截止日期统一按真实日历日期计算，不再使用 Case 任务旧演示相对天数作为到期依据。
- 已结案/已归档 Case 的执行任务仍可查，保持其原状态并标明“Case 已结案”；未完成任务不被自动完成，列表与速览不提供继续执行按钮。
- 按最新要求，移除“显示已升级的原任务”选项及对应条件标签。“我的任务”始终隐藏已升级的原日常任务，不计入列表、关系及状态汇总；仅由这些原任务提供的业务对象与分派人也不再出现在筛选候选中。
- 列表、分组与分页统一显示当前任务数量，不再附加“另显示已升级原任务”计数。原任务数据、附件、评论及升级关系仍保留，不删除、不回退升级，已有详情与关联跳转不变。

### 2.3 卡片与速览

- 任务展示沿用 V1.0.7：待接受任务为双栏派单气泡卡片，进行中、已完成及其他状态为紧凑横向任务行。混合列表按各条任务状态呈现，保留现有排序、分组及每页最多8条的规则。
- 待接受卡片显示实际分派人、接收人、任务标题、日期和业务背景，下方保留有权限的接受/拒绝按钮；其他状态横向排列状态、名称、关联背景、负责人、日期与可用操作。
- 活跃且可继续处理的任务显示紧急程度色条，终态、已升级原任务及已结案 Case 不显示；保留附件数量、语音标识和关联 Case 入口。
- 卡片补充具体业务对象；存在多个关联时简要显示并在速览中展开，不堆叠新的任务类型标签。
- 点击任务名称打开右侧“任务速览”，保留底层筛选、分组和页码；显示负责人、协作者、日期、业务关系、说明、附件、结果、日志和评论。
- 速览直接读取同一任务的最新数据，不保存另一份任务副本。负责人可接受或填写理由拒绝待接受任务；完成、中止、重做、复杂文档编辑仍通过“进入完整详情”操作。
- 速览支持键盘焦点管理和关闭；附件预览单独管理焦点，Escape 先关闭附件再返回速览。评论与接单写入原任务，刷新保留。
- 根据状态与权限显示“接受并开始”“拒绝”“处理任务”“完成任务”等入口。
- **移除每张卡片上的“日常任务 / Case 任务”标签及对应小图标**，避免与顶部来源页签重复。
- 本次移除仅针对卡片来源标签，不删除来源切换、状态或关联信息。

### 2.4 页面视觉精简

- 标题与关系切换同排，取消重复的标题图标与分隔线。
- 三个状态模块保留为独立可选卡片，改为图标、状态说明、数量横向排列，标准桌面高度约92px，减少首屏占用。
- 任务归属使用下划线页签，与右上方人员关系切换区别；业务筛选按需展开，分组选择增加明确的“分组”标签。
- 筛选区域采用轻浅底色，已选条件可逐项清除，数量靠右；不恢复页面搜索或新增状态筛选。
- 业务筛选展开区保持紧凑单行布局，下拉框高度为32px；移除升级原任务选项后，其余三个/四个下拉字段均分宽度，不留空位。
- 按最新要求，任务卡片恢复 V1.0.7 的白底细边框、圆角、左侧紧急程度色条；待接受恢复浅灰派单气泡与蓝色主按钮，其他状态恢复紧凑横向列表，不回退顶部三个状态模块与业务筛选布局。
- Case 等级、业务对象和 Case 名称以紧凑背景标签展示。编号、说明、结论及分派背景保留在标题悬停与速览中，不继续使用独立摘要和卡片底部元信息区域。
- 新版的日常任务、升级原任务与只读历史状态沿用当前数据及权限；不恢复旧版推测分派人、演示完成日期或无处理逻辑的按钮。
- 本次仅调整展示层，人员关系、状态计数、业务筛选、分组分页、历史记录和操作权限不变。

## 3. 新建日常任务

### 3.1 独立创建页面

点击顶部“新建”后，居中遮罩弹窗并排展示“新建 Case”和“新建任务”两个入口，不再使用下拉菜单。选择后关闭弹窗，进入对应的独立创建页面；新建任务适合日常工作记录与分派，不另设内容分类或模板。

- 两个入口各补充三条范例，帮助判断：Case 适合良率下降原因排查、客户功能失效分析、兼容性问题定位与改善验证；任务适合准备评审资料、整理会议纪要与待办、设备点检与结果记录。范例只作选择说明，不增加分类、模板或预填数据。
- 支持点击遮罩、右上角关闭按钮或 Escape 取消选择，取消后保留底层页面与草稿。
- 弹窗内限制键盘焦点，关闭后返回“新建”按钮；进入创建页时保留该页面的初始焦点逻辑。

- 每次创建一条独立任务，不在日常任务内拆分子任务。
- 创建成功后进入该任务详情；取消返回我的任务。
- 新建任务中的“期望完成时间”改为“截止日期”，日常详情和升级摘要同步使用该文案。

### 3.2 默认简单模式

每次打开新建页面时，“高级查找”默认关闭，直接选择负责人。

| 字段 | 说明 |
| --- | --- |
| 任务名称、任务说明 | 必填，说明需要处理的事项与完成标准 |
| 任务负责人 | 可选择自己，或为他人创建 |
| 紧急程度、截止日期 | 必填 |
| 协作者 | 选填，紧邻负责人设置；选择框与已选人数、重叠头像、姓名及部门悬停提示均与新建 Case 共用设计 |
| 任务涉及 | 产品 / Project / TF / 无，两种模式都可选择；非“无”时选择对应对象 |
| 可见范围 | 参考新建 Case：全员可见 / 部分人可见；部分人时选择人员 |
| 附件、语音备注 | 选填，语音入口位于任务说明文本框右上方 |

- 为自己创建的任务初始为“进行中”。
- 为他人创建的任务初始为“待接受”，由负责人确认后开始。
- 简单模式只按直接选择的负责人创建，不触发隐藏的分类分派规则或高级字段校验。

### 3.3 高级查找

“高级查找”（原“按条件找负责人”）开关位于“任务分派”标题栏右上角，不再单独占用一整行。仅用于“不知道具体负责人，但知道处理部门或问题分类”的分派条件，不是另一种任务类型。默认关闭；已知负责人时直接选择。此次仅调整名称与位置，开关语义、找人规则、校验和草稿保留不变。

- 展开后移除“按部门或 Domain + 类型，自动确定负责人”和“发起部门 PTE · 创建人 张伟”两行说明，直接显示负责人确定方式；创建人与发起部门仍自动保存，不改变分派逻辑。

| 字段 | 规则 |
| --- | --- |
| 发起部门 | 根据创建人自动保存，表单不再单独显示 |
| 负责人确定方式 | 按部门 / 按 Domain + 类型 |
| 按部门 | 选择处理部门后，由该部门 leader 接收，不需要填写 Domain 或类型 |
| 按 Domain + 类型 | 两项均需选择，自动匹配负责人及其部门，不需要先选择处理部门 |
| 任务负责人 | 展示自动匹配结果；如果已知道具体人，可切回简单模式直接选择 |

共有规则：

1. 任务涉及、协作者、可见范围属于共有信息，均不随“高级查找”开关隐藏。
2. 不参与当前找人方式的分派草稿不影响提交，切换方式时保留草稿。
3. 可见范围沿用 Case 的全员/部分人选项及上次表单预设；任务保存成功后再更新预设，详情显示已保存设置。
4. **经确认，本轮仅保存可见范围设置，不限制列表、搜索或详情访问。** 任务升级后，各条设置保留在来源上下文中，不自动改变 Case 权限；访问控制后续与 Case 一起完善。
5. 当前分类分派规则和 TF 清单为**明确标注的演示配置**；旧任务不按新规则重新分派。

### 3.4 最终排版与草稿保留

- 简单、高级模式均采用已确认的“左右双栏”布局。
- 左侧为任务内容，右侧为任务分派及可见范围。“任务涉及”合并到任务内容中、紧随说明，不再占用单独模块。
- 新建 Case（含实验版）与新建任务统一使用“协作者”标签、占位文案；任务不再使用独立的姓名标签样式，已选人员与 Case 一样展示人数和头像，可重新打开选择框勾选或取消选择。任务详情与速览的人员属性标签也统一为“协作者”；可见人员选择、“我参与”入口与历史日志不变。
- 附件位于底部通栏，使用虚线拖放上传框：支持多个文件拖入或点击选择，显示拖入高亮、读取状态、文件清单与逐项移除；失败保留原有附件并提示重试。
- 语音备注入口紧邻任务说明文本框右上角，保留录音、播放、删除及重录。
- 任务说明默认采用紧凑高度，支持手动调整，不再占据过高区域。
- 切换模式保留共享内容、附件和语音草稿；两种模式的负责人选择分别保留。
- 关闭“高级查找”后，隐藏的分派条件不参与直接选人提交；任务涉及与可见范围仍参与验证和保存。
- 新建任务底部只保留取消、创建任务按钮，不再展示“一条任务，不拆分子任务”等规则说明话术；任务层级规则不变。

## 4. 日常任务处理与 Workspace

### 4.1 状态和操作

日常任务的执行状态为：**待接受 / 进行中 / 已拒绝 / 已完成 / 已中止**。

| 操作 | 适用状态与权限 | 结果 |
| --- | --- | --- |
| 接受并开始 | 待接受，任务负责人 | 转为进行中 |
| 拒绝 | 待接受，任务负责人；必须填写理由 | 转为已拒绝并保留理由 |
| 保存 Workspace | 进行中，任务负责人 | 保存文档，不自动完成任务 |
| 完成 | 进行中，任务负责人；必须有已保存的非空结论 | 转为已完成；结果附件选填 |
| 中止 | 待接受或进行中，任务负责人；必须填写原因 | 转为已中止，不要求先填写结论 |
| 退回重做 | 已完成，任务发起人或负责人主管 | 同一任务重新进入进行中 |

“已升级为 Case”是来源关联标记，**不是新增执行状态**；升级后保留原状态，后续在 Case 内处理。

### 4.2 Workspace 文档

- 新增日常任务的“分析过程与结论”文档入口。
- 支持编辑任务结论及纯文本分析过程，点击“保存并回写”后持久保存。
- 保存过程草稿不等于完成任务；可先记录过程，再补充结论。
- 关闭有未保存修改的 Workspace 时，提供确认提示。
- 非负责人、非进行中任务及已升级来源任务只能查看日常任务 Workspace。
- 保存失败时显示错误，保留当前编辑内容，不提示虚假成功。

### 4.3 任务附件与结果附件

- 创建时的附件和语音作为任务输入材料展示。
- 日常任务的结果附件保留在结果区域，与创建时的输入附件区分。
- 支持显示文件名、类型及可用文件的预览或下载入口；具体预览方式取决于文件类型。
- 历史记录若只有文件名、没有文件内容，则明确提示并禁用不可用的预览入口。
- Case 新增执行任务时保留真实附件与语音数据，不再只展示占位文件名。

## 5. 一条或多条日常任务升级为 Case

在日常任务详情点击“升级为 Case”，可仅带入当前任务，也可选择其他有权限的日常任务，一起建立新 Case。

- 由任务负责人或创建人操作；已中止或已升级任务不能再次正常发起升级。
- 新 Case 以当前任务的名称及基础信息为主。
- 所选任务放入“带入的日常任务”分组，各自成为执行任务。
- 保留每条任务的负责人、状态、说明、参与人、日期、拒绝及重做原因等已有信息。
- 带入已保存结论、分析过程、输入附件、结果附件和语音；输入与结果文件均保留在导入的执行任务附件中。
- 保留发起部门、分类、涉及对象等来源上下文，以及评论、回复和已记录日志。
- 原日常任务保留为来源记录，显示关联 Case 入口，不再重复执行或继续添加评论。
- 后续修改 Case 任务，不反向改写原日常来源的执行状态。
- 若新 Case 已保存但来源关联保存失败，重试恢复同一批次，避免重复创建 Case。

## 6. 任务详情日志、评论与退回重做

以下能力同时覆盖日常任务详情与 Case 执行任务详情。

### 6.1 任务流转日志

- 使用参考 Case 日志的时间线样式，显示操作摘要、操作人及时间。
- 记录已保存的创建、状态流转、字段修改、升级和评论等操作。
- 展示字段修改前后值；较长内容支持展开查看完整差异。
- 按最新在前排序，已有状态历史与新日志去重后展示。
- **不再单独显示“流转记录 / 修改记录”筛选，统一在“全部”中展示。**
- 相同内容重复保存不生成重复修改日志。
- 早期没有记录的操作不补造，缺少历史时显示空状态。

### 6.2 评论与回复

- 保留“操作日志 / 评论”两个页签。
- 支持纯文本评论和一级回复，每条最多 2,000 字符。
- 主评论输入框支持 Enter 换行、Ctrl / Command + Enter 发送，并处理输入法组合输入。
- 评论和回复保存在当前浏览器，刷新后可查看。
- 任务完成后仍可讨论；评论权限不等于编辑任务或改变状态的权限。
- 发送失败时保留草稿；切换日志与评论页签不会清空主评论草稿。
- 已升级的日常来源评论只读，后续讨论在关联 Case 的对应任务中继续。

### 6.3 退回重做

- 仅已完成任务可操作，必须填写原因，最多 2,000 字符。
- 有权限的人员为**任务发起人，或任务负责人的实际直属主管**。
- 负责人本人不会仅因“是负责人”而自动获得此权限；若同时是发起人，则可操作。
- Case 执行任务以任务分派人作为发起人，不以 Case 创建人自动替代。
- 退回后在**同一条任务**上转为进行中，不创建新的重做任务副本。
- 保留任务身份、负责人、原结论、过程、附件、评论及历史记录；Case 任务的内部进度重置为 0。
- 每次退回原因均进入日志，后续可以继续处理并重新完成。
- 已升级来源任务及已结案、已归档 Case 中的任务不可通过此入口重新开启。

### 6.4 最终详情布局

日常任务详情的“任务参与人”改为“协作者”，任务速览同步；删除属性区重复的截止日期，仅保留任务信息右上角的日期显示，日期数据与处理规则不变。速览原本只有一处截止日期，保留显示。

1. 任务信息和任务附件连续展示，附件紧随基本属性。
2. 拒绝理由、中止原因、升级提示等放在输入材料之后，不再隔开基本信息与附件。
3. 日常任务提供 Workspace 与结果附件区域；Case 任务保留分析任务视图、已有状态时间线等内容。
4. **任务流转日志作为详情内容的最后一个模块**，评论仍位于该模块内。
5. 右下方操作按钮统一为：**取消 / 中止 / 退回重做 / 完成**。
6. 取消只返回上级页面，不改变任务状态；无权限或状态不适用的按钮保留显示但禁用，并提供原因说明。

已升级日常任务的升级信息采用紧凑两行提示条：第一行并排展示升级标识、Case 名称与编号，第二行简要说明来源保留和后续处理位置。移除原先的独立大面积 Case 链接区及重复提示；长 Case 名称省略显示，悬停可查看完整名称，点击仍可进入关联 Case。

## 7. DIMM / CAMM 与 IC Case 关联修复

- 从 DIMM / CAMM Case 拆分建立 IC Case 时，保留明确的模块来源。
- 模块 Case 与每个拆分 IC Case 双向显示关联，覆盖单个 IC 和批量多个 IC 的场景。
- 创建与关联保存失败时，不保留半保存的 Case 或关系。
- 对符合条件的旧记录按显式来源、父子及已有关系字段执行一次性回填。
- 不按名称猜测关联，不自动扩展到 Fanout 或非 IC 场景。
- 已有关联去重但保留原关系信息；明确解除关联后，不因刷新又自动恢复。

此项是对已有 Case 关联能力的修复与持久保存补全。

## 8. 全局 EA Agent

- 新增蓝紫色机器人悬浮入口，可在页面内拖动并记住位置。
- 具备视口边界限制；拖动与点击打开聊天相互区分。
- 点击打开小型聊天窗口，可展开为整页、还原、收起或按 Escape 关闭。
- 展开、还原和页面内切换时保留当前对话及输入草稿，刷新页面后对话清空。
- 不因聊天窗口展开而重建底层业务页面或丢失未提交表单。
- 当前回复来自本地演示规则，**未接入真实 AI 服务，不会自动查询或修改业务数据**。
- 多轮图标设计仅用于独立比较，未应用；正式入口继续使用机器人图标。

## 9. 日常任务范例补充

本版原有 5 条初始范例，本轮再追加以下 6 条：

| 初始状态 | 范例名称 | 创建人 | 参与人 | 紧急程度 | 首次补齐时的截止日期 |
| --- | --- | --- | --- | --- | --- |
| 待接受 | 跨部门评审会议资料收集 | 刘洋 | 李娜 | 紧急 | 当日后 1 天 |
| 待接受 | 样品借用与归还清单确认 | 李娜 | 周杰 | 一般 | 当日后 2 天 |
| 待接受 | 新人实验室入场准备 | 周杰 | 无 | 一般 | 当日后 3 天 |
| 进行中 | 测试数据共享目录整理 | 当前用户 | 周杰 | 紧急 | 当日后 1 天 |
| 进行中 | 常用测试脚本使用说明更新 | 当前用户 | 李娜 | 一般 | 当日后 3 天 |
| 进行中 | 下周设备维护窗口协调 | 当前用户 | 周杰 | 一般 | 当日后 4 天 |

- 新增范例负责人均为当前演示用户张伟，便于在“我负责”中查看和操作。
- 全新数据环境共 11 条范例，初始为 6 条待接受、5 条进行中。
- 老数据环境仅按固定 ID 补齐缺少的范例，不覆盖用户任务或已操作范例。
- 已接受、已拒绝、已完成、已中止或已升级的范例，刷新后不会被重置回初始状态。
- 初始范例状态不伪装成真实历史日志，后续实际操作按正常规则记录。

## 10. 当前实现边界

- 当前为 **PC 桌面端前端原型**，不包含移动端适配验收。
- 任务、日志、评论及相关演示数据主要保存在当前浏览器，不代表已接入后端、真实登录权限或多人协同服务。
- 页面内权限校验用于原型交互，正式上线仍需服务端权限控制。
- 浏览器存储空间影响附件及记录保存，清除浏览器数据可能丢失本地内容。
- 分类负责人映射与 TF 清单使用演示配置；EA Agent 使用离线演示回复。
- 本版持久化评论指任务详情中的新评论路径，不表示所有旧思维导图局部评论交互都已迁移。
- Case 级既有演示日志未整体替换；任务日志仅参考其视觉样式。
- 独立图标、任务标签等比较页不等于正式版本已采用的设计。

## 11. 验证与交付

### 11.1 自动化验证

截至本文更新，**101 项自动化测试全部通过**，主要覆盖：

- V1.0.7 风格的任务气泡/紧凑行展示、各状态和人员关系下的操作权限、真实分派人与信息保留。
- 升级原任务勾选保留状态、共同筛选与独立计数、混合/仅历史记录跨页统计，以及“全部 / Case 任务 / 日常任务”切换的 Case 条件保留与清除。
- 新建 Case / 任务共用协作者选择器的空态、人数、头像与部门提示一致性。

- 新建任务简单 / 高级模式、分派规则及字段保存。
- 按部门 / Domain + 类型独立确定负责人，两种模式的任务涉及与可见范围设置保存、表单预设兼容及失败保护。
- 任务状态操作、Workspace、结论及附件保留。
- 拖放与点击选择共用的附件读取逻辑、文件类型和内容保留、整批读取失败及重试。
- 工作台关系与参与口径、三个状态计数、业务引用、历史 Case 的只读执行边界、升级来源去重、搜索、日期计算及分组分页。
- 单任务 / 多任务升级、失败重试与来源关联。
- 日志真实性、统一展示、历史去重、评论及保存失败保护。
- 退回重做的权限、原因校验和原内容保留。
- 新范例补齐、重复加载不重复插入、既有记录与已处理状态保护。
- DIMM / CAMM 与 IC Case 关联修复。
- EA Agent 位置边界与离线回复说明。

### 11.2 交付文件

| 文件 | 用途 |
| --- | --- |
| [V1.0.8.html](V1.0.8.html) | 正式单文件离线演示版 |
| [app/EA系统-离线版.html](app/EA系统-离线版.html) | 与正式离线版保持一致的应用内导出副本 |
| [web/index.html](web/index.html) | Web 版入口，配套静态资源同步更新 |
| [v1.0.8修改内容说明.md](v1.0.8修改内容说明.md) | 本版新增与修改内容说明 |

本次同步仅更新 V1.0.8 交付物，保留 [V1.0.7.html](V1.0.7.html) 及更早版本。