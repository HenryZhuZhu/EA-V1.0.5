# V1.0.6 修改内容说明

> 文档日期：2026-08-21  
> 适用文件：`V1.0.6.html` 及当前 EA V1.0.5 源码分支  
> 说明：本文汇总本轮已确认并落地的最终功能。讨论过程中产生但已被回退的中间样式不计入正式功能。

## 1. 版本概览

本轮修改主要集中在以下五个方向：

1. Fanout Case 的生成、筛选、状态与结案流程。
2. Case 状态体系及 Case 详情任务状态统计。
3. 分析中心产品分析的周期趋势与联动分析。
4. Case 关联入口及 AI 建议交互整合。
5. Coverage 上线任务字段补充。

另外保留了两份独立产品分析研究原型：

- `产品分析_问题解决效率原型.html`
- `产品分析_周期趋势诊断原型.html`

这两份文件用于统计口径和交互方案探索，不属于正式 V1.0.6 页面入口。

---

## 2. Fanout 相关修改

### 2.1 Fanout Case 统一列表

“我的 Case”中的 Fanout 不再区分：

- 我 Fanout 给别人的
- 别人 Fanout 给我的

现在统一展示为“与我相关的 Fanout 验证 Case”。

统一列表包含：

- 当前用户发起并分配给其他负责人的 Fanout Case。
- 其他用户发起，当前用户作为 Owner 或参与人的 Fanout Case。

同一个 Case 只展示一次。

### 2.2 全部 Case 增加“Fanout我”筛选

分析中心的“全部 Case 列表”在范围筛选中增加“Fanout我”，位于“与我相关”之后。

筛选条件为：

- Case 是 Fanout 自动派生 Case；
- 创建人不是当前用户；
- 当前用户是 Case Owner 或参与人。

该筛选可以与层级、紧急度、状态、产品、部门、订阅等筛选组合使用。

### 2.3 Fanout 自动建案

Fanout 会为目标产品自动生成一个新 Case，并将目标产品负责人设置为 Case Owner。

自动 Case 名称统一为：

```text
产品 + Fail Stage + Fail Mode
```

示例：

```text
DDR5-X-6400 · FT · Contact Fail
```

自动带入信息包括：

- 目标产品及产品负责人；
- 源 Case 的层级、紧急度；
- Fail Stage；
- Fail Mode；
- 问题背景、影响范围；
- 根因、结论和解决方案；
- 源 Case 关联关系。

Fanout Case 创建后默认状态为“验证中”。

### 2.4 Fanout Case 单步结案

Fanout 自动生成的 Case 不需要再次执行“确认根因 → Fanout → 结案”的完整流程。

详情页主按钮直接显示“结案”。点击后打开单步结案弹窗，弹窗仅包含：

- 结案结论；
- 取消按钮；
- 结案按钮。

不会显示：

- Domain；
- KR；
- 附件；
- 再次 Fanout；
- 多步骤进度。

提交后直接调用 Case 结案逻辑，不会递归生成新的 Fanout Case。

### 2.5 源 Case 的 Fanout 进度

源 Case 详情继续显示 Fanout 进度，包括：

- 已生成的 Fanout Case 数量；
- 已完成数量；
- 平均完成度；
- 每个目标产品的验证 Case；
- 点击进入对应 Fanout Case。

---

## 3. Case 状态体系

### 3.1 新增“验证中”

Case 展示状态统一为：

- 进行中
- 验证中
- 暂存
- 已结案

状态判定优先级：

```text
已结案 > 验证中 > 暂存 > 进行中
```

具体规则：

1. Closure 状态为 `closed`，或原 Case 状态为已关闭/已结案/已归档：显示“已结案”。
2. 已确认根因并等待验证结案，或原 Case 状态为“验证中”：显示“验证中”。
3. 原 Case 状态为 `Pending`：显示“暂存”。
4. 其他未结案状态：显示“进行中”。

### 3.2 状态筛选

全部 Case 的状态筛选更新为：

```text
全部 / 进行中 / 验证中 / 暂存 / 已结案
```

### 3.3 状态标签覆盖范围

统一 Case 状态标签已应用于：

- 分析中心全部 Case 列表；
- 我的 Case；
- Case 详情；
- 全局搜索结果；
- 演示模式。

“验证中”使用青色标签，与任务“进行中”的蓝色和“已结案”的绿色区分。

---

## 4. Case 详情任务状态统计

Case 详情右侧“协作 / 任务”区域继续展示：

```text
部门数 · 任务总数
```

并在下一行补充各任务状态的非零数量：

- 待接受
- 进行中
- 已完成
- 已拒绝
- 已中止

只统计具有任务编号的执行任务，不统计思路节点或怀疑方向节点。

计算公式：

设当前 Case 的执行任务集合为 $T$，任务状态为 $s(t)$。

任务总数：

$$
N_{task}=|T|
$$

状态 $k$ 的任务数量：

$$
N_k=\sum_{t\in T} I(s(t)=k)
$$

其中 $I(\cdot)$ 为指示函数，条件成立取 1，否则取 0。

示例 Case 当前显示：

```text
5 部门 · 18 任务
8 待接受 · 6 进行中 · 3 已完成 · 1 已中止
```

数量为 0 的状态不显示。

---

## 5. Case 关联功能

### 5.1 统一关联入口

Case 详情中的“AI 建议”和“关联”合并为单一入口：

```text
添加关联 Case
```

AI 推荐数量以独立辅助提示显示，例如：

```text
4 条 AI 建议
```

### 5.2 弹窗结构

统一弹窗包含两个页签：

- AI 建议
- 手动关联

AI 建议页支持：

- 查看相似 Case；
- 查看相似度及判断依据；
- 建立关联；
- 标注重复。

手动关联页支持：

- 按产品筛选；
- 搜索 Case；
- 多选 Case；
- 批量关联；
- 标注重复。

关联关系仅作为关系标记，不会合并或删除 Case 数据。

Fanout Case 不进入普通“关联 Case”列表，统一从源 Case 的 Fanout 进度查看。

---

## 6. Coverage 上线任务

当思维导图或任务视图中的任务类型选择“Coverage上线”时，新增必填字段：

```text
Release type
```

选项为：

- Reject
- Monitor

字段名：

```ts
coverageReleaseType?: "Reject" | "Monitor"
```

该字段会在以下路径中保持一致：

- 思维导图任务编辑；
- WBS/任务视图任务编辑；
- Case 详情任务保存；
- 新建 Case 任务转换。

---

# 7. 分析中心：产品分析

## 7.1 页面定位

产品分析用于回答以下问题：

1. 不同产品的 Case 平均结案耗时如何变化？
2. 当前周期中，哪个部门的参与和任务耗时影响较大？
3. 哪些 Case 耗时较高？
4. 哪些成员的平均处理耗时较高？
5. 选择时间点、产品和部门后，结果如何联动？

## 7.2 页面结构

产品分析恢复为原左右工作台样式。

### 左侧：产品选择

每个产品显示：

- 产品编码；
- 平台和产品族；
- 趋势缩略图；
- 平均结案耗时。

支持同时选择多个产品。至少保留一个产品。

### 页面右上角：全局控制

产品分析页面右上角提供：

- 按天
- 按周
- 按月
- 分析范围

这些控制对所有已选产品统一生效。

### 右侧主面板顶部

显示：

- 当前产品或产品组合；
- 平均结案时间；
- 范围内已结案 Case 数量。

### 左侧图表区

从上到下为：

1. Case 平均耗时趋势折线图；
2. 部门环状图；
3. 部门图例。

多选产品时，每个产品分别显示一条折线，并使用不同颜色。

### 右侧结果区

包含两个页签：

- 高耗时 Case
- 成员平均耗时

并提供搜索框，可搜索：

- Case 编号；
- Case 名称；
- 工程师；
- 任务名称。

## 7.3 时间范围与粒度

默认分析范围为业务最新日期向前 90 天。

自定义范围必须满足：

$$
StartDate \le EndDate
$$

分桶规则：

- 按天：同一自然日为一个桶；
- 按周：周一至周日为一个桶；
- 按月：同一自然月为一个桶。

设 Case $c$ 的实际结案时间为 $close(c)$，则它所属时间桶为：

$$
Bucket(c)=BucketKey(close(c), grain)
$$

## 7.4 Case 结案耗时

设：

- $created(c)$：Case 创建时间；
- $close(c)$：Case 实际结案时间。

Case 结案耗时按小时计算：

$$
H_c=\max(12, close(c)-created(c))
$$

当前离线演示按日期级数据计算，最小值设置为 12 小时，避免同日创建并结案显示为 0。

天数显示值：

$$
D_c=\frac{H_c}{24}
$$

## 7.5 单产品周期平均耗时

设产品 $p$ 在时间桶 $b$ 内完成的 Case 集合为 $C_{p,b}$。

该产品在该时间桶的平均耗时为：

$$
\bar{H}_{p,b}=\frac{\sum_{c\in C_{p,b}}H_c}{|C_{p,b}|}
$$

折线图中的一个点对应一个 $\bar{H}_{p,b}$。

## 7.6 多产品折线

选择多个产品后，对每个产品分别计算：

$$
Series_p=\{\bar{H}_{p,b_1},\bar{H}_{p,b_2},...,\bar{H}_{p,b_n}\}
$$

不同产品使用不同颜色：

- 第一个产品：品牌蓝 `#0052D9`；
- 后续产品：青色、绿色、橙色、红色等区分色。

图例显示产品编码与对应颜色。

产品之间不会先求平均再绘制一条合并曲线；每个产品保留自己的趋势。

## 7.7 顶部平均结案时间

设分析范围内所有已选产品的 Case 记录集合为 $C_R$。

顶部平均结案时间为加权总体平均：

$$
\bar{H}_{range}=\frac{\sum_{c\in C_R}H_c}{|C_R|}
$$

该指标基于底层 Case 记录计算，不是“各产品平均值的简单平均”，因此不会让小样本产品获得与大样本产品相同的权重。

## 7.8 部门环状图

当前恢复版页面延续原交互口径：扇区表示部门参与 Case 的占比，并同时保留部门任务总耗时和部门平均耗时数据，用于右侧联动。

设当前产品与时间筛选下，部门 $d$ 参与的去重 Case 数为 $N_d$。

部门占比为：

$$
P_d=\frac{N_d}{\sum_j N_j}\times100\%
$$

一个 Case 可涉及多个部门，因此分母是各部门参与 Case 数之和，不等于去重 Case 总数。

部门任务总耗时：

$$
H_d=\sum_{t\in T_d}H_t
$$

部门平均耗时参考值：

$$
\bar{H}_d=\frac{H_d}{N_d}
$$

如果后续产品定义将环图严格调整为“任务耗时占比”，推荐采用：

$$
P_d^{time}=\frac{H_d}{\sum_j H_j}\times100\%
$$

## 7.9 任务耗时

若任务有完整状态历史，则任务耗时取第一条状态时间到最后一条状态时间：

$$
H_t=LastStatusAt(t)-FirstStatusAt(t)
$$

当前离线演示中，缺少历史时间的任务会使用稳定的模拟耗时，以保证原型能够展示环图和联动。生产环境应由任务状态时间戳直接计算。

## 7.10 成员平均耗时

在当前产品、时间点和部门筛选下，设成员 $m$ 的任务集合为 $T_m$，参与的去重 Case 集合为 $C_m$。

成员任务总耗时：

$$
H_m=\sum_{t\in T_m}H_t
$$

当前页面的成员平均耗时按参与 Case 数归一：

$$
\bar{H}_m=\frac{H_m}{|C_m|}
$$

列表同时显示：

- 成员参与 Case 数；
- 成员任务数；
- 成员平均耗时。

点击 Case 数可展开该成员关联的 Case；点击任务数可展开任务明细。

## 7.11 高耗时 Case

高耗时 Case 按 Case 耗时从高到低排序：

$$
Rank(c)=Descending(H_c)
$$

同一个 Case 在历史演示记录中可能有多个周期样本，右侧列表按 Case ID 去重，并保留筛选范围内的最大耗时：

$$
H_c^{display}=\max(H_{c,1},H_{c,2},...,H_{c,n})
$$

## 7.12 联动逻辑

### 仅选择折线点

点击产品 $p$ 在时间桶 $b$ 的点：

```text
产品 = p
时间桶 = b
部门 = 全部
```

右侧高耗时 Case 和成员平均耗时仅使用该产品、该时间桶的数据。

### 仅选择部门

点击部门环图或图例中的部门 $d$：

```text
产品 = 当前已选产品集合
时间 = 当前分析范围
部门 = d
```

右侧只保留该部门参与的 Case 和任务。

### 同时选择折线点和部门

两个条件取交集：

$$
Result=C_p\cap C_b\cap C_d
$$

页面筛选标签显示：

```text
产品 · 时间点 · 部门
```

例如：

```text
DMJFC · 5/22 · 部门 · PTE
```

### 取消选择

- 再次点击同一个折线点：取消产品和时间点筛选，保留部门筛选。
- 再次点击同一个部门：取消部门筛选，保留产品和时间点筛选。
- 点击右侧筛选标签的关闭按钮：清除全部图表筛选。

### 产品多选变化

增加或移除产品时：

- 重新计算各产品折线；
- 重新计算顶部汇总；
- 重新计算部门环图；
- 清除原图表联动条件；
- 刷新右侧结果。

## 7.13 离线演示数据说明

正式业务口径应使用：

- Case 实际创建时间；
- Closure 实际结案时间；
- 任务状态历史时间戳。

当前离线 HTML 中，如果某产品缺少足够历史闭环记录，会生成稳定、可重复的演示记录，以确保多产品趋势和联动可以被验收。该兜底逻辑只用于原型演示，不应作为生产统计来源。

---

## 8. 新建 Case 图标导出

本次同时导出新建 Case 中以下三个图标：

| 功能 | 图标来源 | 文件 |
|---|---|---|
| Chip DNA | Lucide `Fingerprint` | `Chip DNA.svg` |
| OneClick | Lucide `MousePointerClick` | `OneClick.svg` |
| CP Index | Lucide `Hash` | `CP Index.svg` |

SVG 保留 Lucide 标准 `24 × 24` viewBox、2px 描边、圆角端点和连接点。默认描边色使用当前按钮文字色 `#475569`。

---

## 9. 相关文件

主要源码：

- `app/src/app/components/CaseList.tsx`
- `app/src/app/components/ProductAnalysisRestored.tsx`
- `app/src/app/components/CaseStatusBadge.tsx`
- `app/src/app/components/CaseDetail.tsx`
- `app/src/app/components/CaseRelations.tsx`
- `app/src/app/components/CoveragePatternFields.tsx`
- `app/src/app/components/domainStore.ts`
- `app/src/app/components/CloseCaseModal.tsx`
- `app/src/app/components/data.ts`

交付文件：

- `V1.0.6.html`
- `v1.0.6修改内容说明.md`
- `Chip DNA.svg`
- `OneClick.svg`
- `CP Index.svg`
